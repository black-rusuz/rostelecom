name: rostelecom
description: Case for DSTU Hackathon Autumn 2022
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  cupertino_icons: ^1.0.2
  flutter_svg: ^1.1.5
  google_fonts: ^3.0.1
  cached_network_image: ^3.2.2

  dio: ^4.0.6
  get_it: ^7.2.0
  injectable: ^1.5.3

  rxdart: ^0.27.5
  flutter_bloc: ^8.1.1
  equatable: ^2.0.5

  flutter_native_splash: ^2.2.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

  injectable_generator: ^1.5.4
  build_runner: ^2.2.1
  flutter_launcher_icons: ^0.10.0

flutter:
  uses-material-design: true

#  assets:
#    - assets/bg.svg
#    - assets/login_bg.svg
